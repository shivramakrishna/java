// Generated by CoffeeScript 1.12.7
(function() {
  var Caf, MyCustomClass, defineModule, describe, each, formattedInspect, ref, ref1, test, tests;

  ref = require('art-standard-lib'), each = ref.each, defineModule = ref.defineModule, formattedInspect = ref.formattedInspect;

  ref1 = require("art-testbench"), describe = ref1.describe, test = ref1.test;

  Caf = require("../../");

  MyCustomClass = (function() {
    function MyCustomClass() {}

    return MyCustomClass;

  })();

  tests = [[(function() {}), Function], [1, Number], [{}, Object], ["", String], [/-/, RegExp], [true, Boolean], [false, Boolean], [[], Array], [null, null], [void 0, void 0], [new MyCustomClass, MyCustomClass]];

  each(tests, function(arg) {
    var klass, obj;
    obj = arg[0], klass = arg[1];
    return each(tests, function(arg1) {
      var testKlass, testObj;
      testObj = arg1[0], testKlass = arg1[1];
      if (klass === testKlass) {
        return test((formattedInspect(obj)) + " is " + (testKlass && testKlass.name), function() {
          return assert.ok(Caf.is(obj, testKlass));
        });
      } else {
        return test((formattedInspect(obj)) + " isnt " + (testKlass && testKlass.name), function() {
          return assert.ok(!Caf.is(obj, testKlass));
        });
      }
    });
  });

}).call(this);
