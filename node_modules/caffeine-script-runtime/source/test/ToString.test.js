// Generated by CoffeeScript 1.12.7
(function() {
  var Caf, defineModule, describe, inspect, log, ref, ref1, test, testMap, testToString;

  ref = require("art-standard-lib"), defineModule = ref.defineModule, log = ref.log, inspect = ref.inspect;

  ref1 = require("art-testbench"), describe = ref1.describe, test = ref1.test;

  Caf = require("../../");

  testToString = function(input, out) {
    return test("toString " + (inspect(input)) + " >> " + (inspect(out)), function() {
      return assert.eq(out, Caf.toString(input));
    });
  };

  testMap = function(map) {
    var k, results, v;
    results = [];
    for (k in map) {
      v = map[k];
      results.push(testToString(v, k));
    }
    return results;
  };

  testMap({
    1: 1,
    hi: 'hi',
    '': [],
    '123': [1, 2, 3],
    '[object Object]': {}
  });

  testMap({
    '': void 0
  });

  testMap({
    '': null
  });

  testMap({
    '': [null]
  });

  testMap({
    '1123aha': [
      [1], 123, {
        toString: function() {
          return 'aha';
        }
      }
    ]
  });

  testMap({
    '12': [null, 1, void 0, 2]
  });

}).call(this);
